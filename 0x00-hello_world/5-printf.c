#include <stdio>
/**
 * main - a program that prints a new line with the printf command
 * Return: 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
